#include "main.h"
/**
 * main - entry point
 * discription:
 * Return: is always 0
 */
void print_alphabet_x10(void)
{
	int n;
	int m;

	for (n = 1;n <= 10; n++)
	{
		for (m = 97; j <= 122; m++)
		{
			_putchar(m);
		}
		_putchar('\n');
	}
}
