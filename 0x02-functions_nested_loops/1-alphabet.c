#include "main.h"
/**
 * print_alphabet - Make the alphabet
 * Return: is always 0
 * print_alphabet - Make the alphabet
 */
void print_alphabet(void)
{
	int n;

	for (n = 97; n <= 122; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}

